import ForgotPasswordClient from "@/components/auth/ForgotPasswordClient";

export const metadata = { title: "Forgot password — Nookly" };

export default function ForgotPasswordPage() {
  return <ForgotPasswordClient />;
}
